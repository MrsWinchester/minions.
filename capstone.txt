CAPSTONE SUBMISSION — L3T10 (Django + Sphinx + Docker)

Canonical repo: https://github.com/MrsWinchester/minions1
Commit (mark this one): <SHORT_HASH_HERE>

Open in that repo:
- manage.py (repo root)
- band/, minions_site/
- docs/build/html/index.html  (Sphinx HTML committed; modules.html also there)
- Dockerfile, requirements.txt

How to run (summary; full steps in README):
py -m venv .venv && .\.venv\Scripts\Activate
pip install -r requirements.txt
py manage.py migrate
py manage.py runserver  → http://127.0.0.1:8000
